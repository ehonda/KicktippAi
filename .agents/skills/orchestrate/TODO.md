# TODO

- Reintroduce an orchestration hook-readiness check after there is a reliable
  way to prove that the exact repository hook definition is active for the
  exact current session. Forward-test first invocation, resumed goals, ordinary
  prompts, and hook-definition changes without making missing prompt-context
  injection an unworkable startup blocker.
