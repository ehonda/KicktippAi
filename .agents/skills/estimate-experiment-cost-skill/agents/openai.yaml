interface:
  display_name: "Experiment Cost Estimator"
  short_description: "Estimate KicktippAi experiment costs"
  default_prompt: "Use $estimate-experiment-cost-skill to estimate a KicktippAi experiment cost from the JSON store, run mandatory preflights for high-reasoning missing rows, and upsert observed base estimate rows when needed."
