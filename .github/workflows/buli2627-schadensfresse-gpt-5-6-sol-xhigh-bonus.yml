name: Bundesliga 2026/27 schadensfresse Sol xhigh CL - Bonus

on:
  workflow_dispatch:
    inputs:
      force_prediction:
        description: 'Force prediction even if verify passes'
        required: false
        default: false
        type: boolean
      max_repredictions:
        description: 'Maximum number of repredictions allowed (0-based index)'
        required: false
        default: 2
        type: number

concurrency:
  group: bundesliga-2026-27-production-live-lane
  cancel-in-progress: false

jobs:
  call-base-workflow:
    name: Strict CL Bonus Predictions
    uses: ./.github/workflows/base-bonus-predictions.yml
    with:
      community: "schadensfresse"
      community_context: "schadensfresse"
      competition: "bundesliga-2026-27"
      model: "gpt-5.6-sol"
      reasoning_effort: "xhigh"
      max_output_tokens: 10000
      prompt_source: "langfuse"
      langfuse_prompt_name: "kicktippai/bundesliga-2026-27/champions-league/predict-bonus"
      langfuse_prompt_label: "production"
      langfuse_prompt_version: 1
      bonus_profile: "schadensfresse-champions-league-bonus-context-free-v1"
      bonus_context_document_budget: 0
      bonus_context_token_budget: 0
      bonus_deadline_at_or_before: "2026-09-08T16:45:00Z"
      trigger_type: "manual"
      force_prediction: ${{ inputs.force_prediction }}
      max_repredictions: ${{ fromJSON(inputs.max_repredictions) }}
    secrets:
      kicktipp_username: ${{ secrets.SCHADENSFRESSE_KICKTIPP_USERNAME }}
      kicktipp_password: ${{ secrets.SCHADENSFRESSE_KICKTIPP_PASSWORD }}
      firebase_project_id: ${{ secrets.FIREBASE_PROJECT_ID }}
      firebase_service_account_json: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}
      openai_api_key: ${{ secrets.OPENAI_API_KEY }}
      langfuse_secret_key: ${{ secrets.LANGFUSE_SECRET_KEY }}
