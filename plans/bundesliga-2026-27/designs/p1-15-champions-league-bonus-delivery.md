# P1-15 frozen CL bonus delivery

The exact tuple is admitted before context or model construction. A strict CL form snapshot validates the three question identities and captures current target selections. Cache candidates are filtered by caller-supplied specialized lineage; ordinary rows remain audit collisions. Generation/reuse produces all three 1/4/1 result sets with no context documents, then a refetched strict form preserves non-target controls and posts once. A fresh strict GET must show all three stored selections before success.

The route is manual-only. Any drift or partial state fails without a POST; rollback is a code/workflow revert and preserves historical Firestore records.
