# P0-12 — Replace transfer context in the match contract

- Status: Complete
- Priority: P0
- Depends on: [P0-09](p0-09-roster-collector.md), [P0-11](p0-11-club-elo-collector.md)
- Decisions: [ADR-0003](../decisions/0003-duckdb-primary-rosters-with-fallback.md), [ADR-0007](../decisions/0007-require-context-hygiene-before-launch.md), [ADR-0010](../decisions/0010-season-scoped-team-identity-manifest.md), [ADR-0011](../decisions/0011-roster-snapshot-and-publication-contract.md), [ADR-0015](../decisions/0015-club-elo-prompt-publication-contract.md), [ADR-0016](../decisions/0016-validate-club-elo-publication-metadata.md), [ADR-0017](../decisions/0017-roster-collector-duckdb-and-reconstruction-contract.md), [ADR-0018](../decisions/0018-validate-roster-publication-metadata-semantically.md), [ADR-0019](../decisions/0019-roster-publication-truth-boundary.md), [ADR-0020](../decisions/0020-record-immutable-match-context-manifests.md), [ADR-0021](../decisions/0021-bind-ordinary-context-content-and-prepare-provenance.md), [ADR-0022](../decisions/0022-transactional-bundesliga-reprediction-allocation.md)

## Outcome

Every 2026/27 match requires the two roster and two Club Elo documents and no live path selects or uploads transfer documents.

## Work items

- [x] Extend `MatchContextDocumentCatalog` with manifest-backed canonical `roster-{slug}` and `club-elo-{slug}.csv` names for both teams.
- [x] Keep standings, community rules, recent history, home/away history, and head-to-head history required for Bundesliga.
- [x] Remove `IncludeTransfers`, transfer optional-document selection, and transfer-specific retrieval branches from matchday, random-match, prompt reconstruction, experiment export/execution, and match-analysis helpers.
- [x] Remove the `upload-transfers` command registration, implementation, tests, and current documentation because it no longer serves a live context contract.
- [x] Simplify `MatchContextDocumentSelection` if optional documents no longer have another supported use.
- [x] Replace transfer-focused tests with assertions that both Elo and roster documents are mandatory and missing required context fails clearly.
- [x] Complete independent review before changing task status.
- [x] Leave historical Firestore documents untouched; do not add a deletion migration.

## Validation

- Run `MatchContextDocumentCatalogTests`, matchday/random-match context retrieval tests, prompt reconstruction tests, and affected experiment/analyze-match tests.
- Search live `src`, `tests`, `.github`, and non-archive docs for transfer-document names and account for every remaining result.

## Implementation evidence

- 2026-08-18: Added ADR-0020 and the shared snapshot-backed resolver. `dotnet build src/Orchestrator/Orchestrator.csproj --no-restore` passed (two existing nullable/obsolete warnings). Full focused and emulator validation remains required before review.
- 2026-08-18: Removed the live upload command, DI registration, prompt file, command tests, and required-only catalog migration. Remaining transfer mentions are being reduced to historical research/ADR wording and rewritten non-live test fixtures; no Firestore deletion is performed.
- 2026-08-18: Validated the immutable resolver and Firestore round trip, including coherent headed snapshots, no generic reserved reads, on-demand ordinary-document materialization with exact reread, and stored-version reconstruction after head advance. Full serial suites passed: Core 120, FirebaseAdapter 241 (emulator), OpenAiIntegration 212, ContextProviders.Kicktipp 46, and Orchestrator 861. `git diff --check` passed. The task remains In progress pending independent review.
- 2026-08-19: Independent-review remediation is in progress: semantic publication reconstruction is mandatory on every resolver boundary; manifests and Firestore reads validate immutable canonical scope/order/identity; provenance persistence blocks Bundesliga submissions when it fails; and Matchday/Verify use exact ordinary versions plus publication heads for outdated checks. Focused resolver tests (6) and Firestore manifest round trip (1) pass. Broader command and prepared-experiment coverage remains pending re-review.
- 2026-08-19: Prepared 2026/27 experiment items support `resolvedContextManifest` plus `predictionCreatedAt`; validation rejects either omission and execution reconstructs only from the recorded snapshots. Legacy 2025/26 prepared artifacts retain timestamp reconstruction. The current outcomes-only `prepare-*` commands intentionally cannot produce 2026/27 artifacts until [P1-06](p1-06-observability-datasets.md) supplies explicit prediction provenance; they fail closed rather than emit unsafe timestamp-only artifacts. Matchday semantic fixtures now assert provenance saves and fail-closed persistence; independent-review/full-suite evidence remains pending.
- 2026-08-19: Review remediation keeps the legacy timestamp-based Verify fixtures explicitly on `bundesliga-2025-26`; 2026/27 Verify now proves that a missing immutable manifest is outdated and that a complete matching manifest/head is current without generic reserved-document reads. The shared checker also rejects reserved-document version advances even when an identical-content snapshot ID is reused. Focused Verify passed 17/17; Core 122/122, FirebaseAdapter 241/241, OpenAiIntegration 212/212, ContextProviders.Kicktipp 46/46, and a prior full Orchestrator run passed 863/863. Final re-review remains required, so the task stays In progress.
- 2026-08-20: ADR-0021 makes ordinary context payloads append-only and transaction-allocated, validates exact stored envelopes, and binds every resolved manifest entry to a SHA-256 content hash. It also records that outcomes-only prepare commands remain fail-closed/manual-provenance until P1-06, while populated file-backed 2026/27 artifacts must execute. Focused Core/Firebase validation is pending this remediation slice's checkpoint.
- 2026-08-20: Open-review remediation continues. Cached 2026/27 predictions now require one matching metadata record and a current canonical manifest before reuse; unsafe normal/cancelled reuse blocks, while repredict follows its explicit next-version/max-limit paths. Prepared manifests now use a strict hash-aware JSON converter and canonical competition/scope validation. Focused Orchestrator coverage passed 20/20 before final added coverage; Core resolver passed 9/9. Firebase and integration emulator execution remains blocked because Testcontainers cannot query Docker (empty JSON response); the Firebase project build passes. The task remains In progress pending the remaining file-backed executor evidence and independent review.
- 2026-08-20: Closed the remaining command-level acceptance evidence: a populated temporary JSON artifact serialized with production options round-trips through the actual prepared-experiment executor and reconstructs only its recorded ordinary v1 payloads and roster/Elo snapshots after current heads advance. Dedicated Matchday coverage proves cancelled unsafe cache blocking, unsafe reprediction generation/persistence/submission, max-limit blocking, and metadata/provenance exception fail-closed behavior; Verify explicitly treats null/corrupt 2026/27 metadata as outdated. The serial focused Orchestrator set passed 110/110. A read-only `docker version` check reports Docker Desktop is unable to start, so the two stale-metadata integration tests remain blocked without changing Docker state. The task remains In progress pending independent review.
- 2026-08-20: Consolidated review remediation adds manifest SHA-256 comparison for every ordinary and selected roster/Elo payload before cached 2026/27 reuse; structural prediction/justification equality; provenance-only 2026/27 adapter writes; and transactionally allocated provenance repredictions with expected-index/max CAS (ADR-0022). Ordinary latest/timestamp/version-list reads now exclude publication-set rows while retaining them in the allocation ceiling. Direct-seeded malformed/reordered-manifest, custom-publication, CAS, normal/cancelled/max cache, and Verify regressions compile. Focused Orchestrator passed 84/84 and Core passed 125/125; FirebaseAdapter test project built successfully. The read-only Docker check still reports `Docker Desktop is unable to start`, so Firebase emulator and integration execution remain blocked without changing Docker state. The task remains In progress pending independent review.
- 2026-08-20: Final independent re-review closed with no findings after four additional remediations: outcomes-only 2026/27 prepare commands reject unsafe artifacts before either file is written; historical manifestless prediction metadata remains readable while new writes stay provenance-only; ordinary name enumeration excludes publication rows; and reprediction allocation cannot overflow at `Int32.MaxValue`. Focused prepare commands passed 9/9, legacy community-to-date passed 1/1, repredict mode passed 10/10, and Core passed 125/125. Full serial non-container acceptance passed OpenAiIntegration 212/212 and ContextProviders.Kicktipp 46/46. The full Orchestrator run passed 888 tests; its only four failures were Firestore fixture initialization failures caused by the unchanged Docker Desktop startup outage. FirebaseAdapter.Tests compiled with zero errors, `git diff --check` passed, the live transfer-path search found only historical/planning/prohibition references, and no Firestore deletion or migration was added. Exact-commit CI will supply the remaining emulator evidence.
- 2026-08-20: Final review remediation validates every prepared 2026/27 bundle before either artifact is written, so outcome-only sources fail closed while the legacy 2025/26 community-to-date path remains supported. Manifestless historical 2026/27 metadata rows remain readable but any non-null manifest is strictly validated; context-name enumeration excludes publication rows; and command plus adapter reprediction allocation cannot overflow `Int32`. Focused prepare-command tests passed 9/9, repredict-mode tests 10/10, Core 125/125, and the FirebaseAdapter test project built with zero errors. Docker Desktop remains unavailable, so emulator execution is still blocked without state changes. The task remains In progress pending independent review.

## Complete when

- A match trace has standings, rules, histories, two Elo rows, and two roster documents.
- No live command asks for `*-transfers.csv` and no upload-transfers command is exposed.
- Future historical experiments remain possible only by explicitly assembling their own context.
